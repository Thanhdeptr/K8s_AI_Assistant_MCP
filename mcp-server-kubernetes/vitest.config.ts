import { defineConfig } from 'vitest/config';nnexport default defineConfig({n  test: {n    globals: true,n    environment: 'node',n    setupFiles: ['./tests/setup.ts'],n    coverage: {n      provider: 'v8',n      reporter: ['text', 'json', 'html'],n      exclude: [n        'node_modules/',n        'dist/',n        'tests/',n        '**/*.d.ts',n        '**/*.config.*',n        '**/*.test.*',n        '**/*.spec.*'n      ]n    }n  },n  resolve: {n    alias: {n      '@': '/src'n    }n  }n});